Algoritmo "TesteSaida"

Var
       a : inteiro
       b : real
       c : caractere
       d : logico
Inicio

      a <- 32
      b <- 2.34596
      c <- "Bom dia!"
      d <- VERDADEIRO
      
      escreval("Saída de Dados:":1)
      escrevaL(c)
      escrevaL(b)
      escrevaL(a)
      escrevaL(d)

FimAlgoritmo